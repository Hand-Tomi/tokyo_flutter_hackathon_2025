# AI 이미지/동영상 생성 플로우차트

## 전체 시스템 아키텍처

```mermaid
flowchart TB
    subgraph USER["👤 사용자 입력"]
        A[텍스트 프롬프트]
        B[참조 이미지 업로드]
    end

    subgraph STAGE1["🎨 1단계: 일관된 이미지 생성"]
        C{이미지 생성 API 선택}
        C -->|1순위| D[🍌 Nano Banana Pro<br/>Gemini 3 Pro Image]
        C -->|2순위| E[Leonardo AI<br/>Character Reference]
        C -->|3순위| F[getimg.ai<br/>DreamBooth]

        D --> G[4장+ 일관된<br/>캐릭터 이미지]
        E --> G
        F --> G
    end

    subgraph FEATURES1["Nano Banana Pro 기능"]
        D1[최대 14개 참조 이미지]
        D2[최대 5명 캐릭터 일관성]
        D3[4K 해상도 지원]
        D --> D1
        D --> D2
        D --> D3
    end

    subgraph STAGE2["🎬 2단계: 출력 형식 선택"]
        H{출력 형식}
        G --> H
        H -->|동영상| I[비디오 생성]
        H -->|움직이는 이미지| J[GIF 생성]
    end

    subgraph VIDEO["📹 비디오 생성 경로"]
        I --> K{비디오 API 선택}
        K -->|1순위| L[Kling 2.1/2.5<br/>캐릭터 일관성 최고]
        K -->|2순위| M[Vidu AI<br/>Multi Entity]
        K -->|3순위| N[Runway Gen-4<br/>스타일화 강점]

        L --> O[1080p 비디오<br/>최대 2분]
        M --> O
        N --> O
    end

    subgraph GIF["🎞️ GIF 생성 경로"]
        J --> P{GIF API 선택}
        P -->|1순위| Q[Magic Hour<br/>최적화된 GIF]
        P -->|2순위| R[Creatomate<br/>프로그래밍 API]
        P -->|3순위| S[PixVerse V4.5<br/>캐릭터 일관성]

        Q --> T[애니메이션 GIF]
        R --> T
        S --> T
    end

    subgraph OUTPUT["📤 최종 출력"]
        O --> U[🎥 MP4/MOV 파일]
        T --> V[🎞️ GIF 파일]
    end

    A --> C
    B --> C

    style D fill:#4CAF50,color:#fff
    style L fill:#2196F3,color:#fff
    style Q fill:#FF9800,color:#fff
    style U fill:#9C27B0,color:#fff
    style V fill:#E91E63,color:#fff
```

---

## 상세 워크플로우

```mermaid
flowchart LR
    subgraph INPUT["입력"]
        A1[프롬프트 작성]
        A2[캐릭터 설정]
        A3[스타일 정의]
    end

    subgraph GENERATE["이미지 생성"]
        B1[Nano Banana Pro API 호출]
        B2[캐릭터 일관성 파라미터 설정]
        B3[4장 이상 생성]
        B4[품질 검증]
    end

    subgraph PROCESS["후처리"]
        C1{만족?}
        C2[재생성]
        C3[이미지 저장]
    end

    subgraph ANIMATE["애니메이션화"]
        D1[Kling API 호출]
        D2[모션 파라미터 설정]
        D3[비디오/GIF 생성]
    end

    subgraph EXPORT["내보내기"]
        E1[포맷 선택]
        E2[해상도 설정]
        E3[파일 저장]
    end

    A1 --> B1
    A2 --> B2
    A3 --> B1
    B1 --> B2 --> B3 --> B4 --> C1
    C1 -->|No| C2 --> B1
    C1 -->|Yes| C3 --> D1
    D1 --> D2 --> D3 --> E1 --> E2 --> E3

    style B1 fill:#4CAF50,color:#fff
    style D1 fill:#2196F3,color:#fff
```

---

## Flutter 앱 통합 아키텍처

```mermaid
flowchart TB
    subgraph APP["📱 Flutter 앱"]
        UI[사용자 인터페이스]

        subgraph SERVICES["서비스 레이어"]
            S1[ImageGenerationService]
            S2[VideoGenerationService]
            S3[GifGenerationService]
        end

        subgraph API["API 클라이언트"]
            A1[NanoBananaClient<br/>dio + REST API]
            A2[KlingClient<br/>dio + REST API]
            A3[MagicHourClient<br/>dio + REST API]
        end

        subgraph CACHE["캐시 레이어"]
            C1[flutter_cache_manager<br/>이미지 캐시]
            C2[로컬 파일 저장소<br/>비디오/GIF]
        end
    end

    subgraph EXTERNAL["🌐 외부 API"]
        E1[Google AI Studio<br/>Nano Banana Pro]
        E2[Kling API<br/>비디오 생성]
        E3[Magic Hour API<br/>GIF 생성]
    end

    UI --> S1 & S2 & S3
    S1 --> A1 --> E1
    S2 --> A2 --> E2
    S3 --> A3 --> E3

    A1 --> C1
    A2 --> C2
    A3 --> C2

    style UI fill:#6750A4,color:#fff
    style E1 fill:#4CAF50,color:#fff
    style E2 fill:#2196F3,color:#fff
    style E3 fill:#FF9800,color:#fff
```

---

## API 호출 시퀀스

```mermaid
sequenceDiagram
    participant U as 👤 사용자
    participant F as 📱 Flutter 앱
    participant NB as 🍌 Nano Banana Pro
    participant K as 🎬 Kling API
    participant MH as 🎞️ Magic Hour

    U->>F: 프롬프트 입력 + 캐릭터 설정

    rect rgb(200, 230, 200)
        Note over F,NB: 1단계: 이미지 생성
        F->>NB: 이미지 생성 요청 (4장)
        NB-->>NB: 캐릭터 일관성 처리
        NB-->>F: 일관된 이미지 4장 반환
        F->>F: 이미지 캐싱
    end

    U->>F: 출력 형식 선택

    alt 동영상 선택
        rect rgb(200, 200, 230)
            Note over F,K: 2-A: 비디오 생성
            F->>K: 이미지 → 비디오 요청
            K-->>K: 캐릭터 일관성 유지 애니메이션
            K-->>F: MP4 비디오 반환
        end
    else GIF 선택
        rect rgb(230, 220, 200)
            Note over F,MH: 2-B: GIF 생성
            F->>MH: 이미지 → GIF 요청
            MH-->>MH: 프레임 최적화
            MH-->>F: 애니메이션 GIF 반환
        end
    end

    F->>U: 최종 결과물 표시
```

---

## 에러 처리 플로우

```mermaid
flowchart TB
    A[API 호출] --> B{응답 확인}

    B -->|성공| C[결과 처리]
    B -->|실패| D{에러 유형}

    D -->|Rate Limit| E[대기 후 재시도<br/>Exponential Backoff]
    D -->|Network Error| F[오프라인 캐시 확인]
    D -->|API Error| G[대체 API 시도]
    D -->|Timeout| H[타임아웃 연장 재시도]

    E --> A
    F -->|캐시 있음| C
    F -->|캐시 없음| I[사용자에게 알림]
    G --> A
    H --> A

    C --> J[로컬 저장]
    J --> K[UI 업데이트]

    style C fill:#4CAF50,color:#fff
    style I fill:#F44336,color:#fff
```

---

## 비용 최적화 플로우

```mermaid
flowchart LR
    subgraph COST["💰 비용 최적화 전략"]
        A[요청 수집] --> B{배치 가능?}
        B -->|Yes| C[배치 API 사용<br/>30-70% 할인]
        B -->|No| D[단일 요청]

        C --> E{해상도 선택}
        D --> E

        E -->|미리보기| F[표준 해상도<br/>$0.134/장]
        E -->|최종본| G[4K 해상도<br/>$0.24/장]

        F --> H[캐시 저장]
        G --> H

        H --> I[중복 요청 방지]
    end

    style C fill:#4CAF50,color:#fff
    style F fill:#2196F3,color:#fff
```

---

## 텍스트 버전 플로우차트

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         🎨 AI 이미지/동영상 생성 플로우                    │
└─────────────────────────────────────────────────────────────────────────┘

                              ┌──────────────┐
                              │  👤 사용자    │
                              │   입력       │
                              └──────┬───────┘
                                     │
                    ┌────────────────┼────────────────┐
                    ▼                                 ▼
            ┌──────────────┐                 ┌──────────────┐
            │ 텍스트 프롬프트 │                 │ 참조 이미지   │
            └──────┬───────┘                 └──────┬───────┘
                    │                                 │
                    └────────────────┬────────────────┘
                                     ▼
┌─────────────────────────────────────────────────────────────────────────┐
│  🍌 1단계: Nano Banana Pro (Gemini 3 Pro Image)                         │
│  ├─ 최대 14개 참조 이미지 지원                                            │
│  ├─ 최대 5명 캐릭터 일관성 유지                                           │
│  └─ 4K 해상도 지원                                                      │
└─────────────────────────────────────────────────────────────────────────┘
                                     │
                                     ▼
                         ┌───────────────────┐
                         │  4장+ 일관된 이미지  │
                         └─────────┬─────────┘
                                   │
                    ┌──────────────┴──────────────┐
                    ▼                             ▼
    ┌───────────────────────────┐   ┌───────────────────────────┐
    │  📹 동영상 생성 경로         │   │  🎞️ GIF 생성 경로          │
    │                           │   │                           │
    │  ┌─────────────────────┐  │   │  ┌─────────────────────┐  │
    │  │ 🥇 Kling 2.1/2.5    │  │   │  │ 🥇 Magic Hour       │  │
    │  │ - 캐릭터 일관성 최고   │  │   │  │ - 최적화된 GIF      │  │
    │  │ - 1080p, 최대 2분   │  │   │  │ - MP4/GIF 내보내기   │  │
    │  └─────────────────────┘  │   │  └─────────────────────┘  │
    │                           │   │                           │
    │  ┌─────────────────────┐  │   │  ┌─────────────────────┐  │
    │  │ 🥈 Vidu AI          │  │   │  │ 🥈 Creatomate       │  │
    │  │ - Multi Entity      │  │   │  │ - 프로그래밍 API     │  │
    │  └─────────────────────┘  │   │  └─────────────────────┘  │
    │                           │   │                           │
    │  ┌─────────────────────┐  │   │  ┌─────────────────────┐  │
    │  │ 🥉 Runway Gen-4     │  │   │  │ 🥉 PixVerse V4.5    │  │
    │  │ - 스타일화 강점      │  │   │  │ - 캐릭터 일관성      │  │
    │  └─────────────────────┘  │   │  └─────────────────────┘  │
    └─────────────┬─────────────┘   └─────────────┬─────────────┘
                  │                               │
                  ▼                               ▼
         ┌──────────────┐                ┌──────────────┐
         │  🎥 MP4/MOV  │                │  🎞️ GIF 파일  │
         │    파일      │                │              │
         └──────────────┘                └──────────────┘


┌─────────────────────────────────────────────────────────────────────────┐
│                           💰 예상 비용 (4장 기준)                         │
├─────────────────────────────────────────────────────────────────────────┤
│  이미지 생성 (Nano Banana Pro 4K)  │  $0.24 × 4 = $0.96                 │
│  비디오 변환 (Kling 2.1)           │  ~$0.50 - $1.00                    │
│  ───────────────────────────────────────────────────────                │
│  총계                              │  ~$1.50 - $2.00                    │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## API 엔드포인트 요약

| 단계 | API | 엔드포인트 |
|------|-----|-----------|
| 이미지 생성 | Nano Banana Pro | `https://generativelanguage.googleapis.com/v1beta/models/gemini-3-pro-image` |
| 비디오 생성 | Kling | `https://api.kling.ai/v1/videos/image2video` |
| GIF 생성 | Magic Hour | `https://api.magichour.ai/v1/gif/generate` |

