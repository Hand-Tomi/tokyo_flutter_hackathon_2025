name: presentation
description: Presentation layer - ViewModels, Pages, Mappers
publish_to: none
resolution: workspace

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # ローカルパッケージ
  domain:
    path: ../domain
  design_system:
    path: ../design_system

  # 状態管理
  flutter_riverpod: ^2.4.0
  riverpod_annotation: ^2.3.0

  # ルーティング
  go_router: ^12.1.3

  # ユーティリティ
  flutter_dotenv: ^5.2.1
  freezed_annotation: ^3.0.0
  intl: ^0.18.0
  uuid: ^4.0.0

  # Hand Tracking
  camera: ^0.11.0+2
  hand_landmarker: ^2.1.2
  permission_handler: ^11.0.1

  # Voice Chat
  openai_realtime_dart: ^0.1.0+1
  record: ^6.0.0
  just_audio: ^0.9.42

dev_dependencies:
  build_runner: ^2.4.6
  freezed: ^3.0.0
  riverpod_generator: ^2.3.0
